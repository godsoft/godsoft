##
#set($voPackage="${voPackage}")
#set($voClassName="${model.entity.pcName}VO")
#set($defaultVoClassName="${model.entity.pcName}DefaultVO")
#set($searchVOAlias="${model.entity.ccName}SerarchVO")
#set($daoPackage="${daoPackage}")
#set($daoClassName="${model.entity.pcName}DAO")
#set($daoName="${model.entity.ccName}DAO")
#set($daoInstanceName="${model.entity.ccName}DAO")
##
##
#set($vender="$model.vender")
#set($namespace="$model.entity.pcName")
#set($resultMapId="${model.entity.ccName}")
#set($resultMapClass="${voPackage}.${voClassName}")
##
##
#set($insertQueryId="${daoInstanceName}.insert${model.entity.pcName}_S")
#set($updateQueryId="${daoInstanceName}.update${model.entity.pcName}_S")
#set($deleteQueryId="${daoInstanceName}.delete${model.entity.pcName}_S")
#set($selectQueryId="${daoInstanceName}.select${model.entity.pcName}_S")
#set($selectListQueryId="${daoInstanceName}.select${model.entity.pcName}List_D")
#set($selectListTotCntQueryId="${daoInstanceName}.select${model.entity.pcName}ListTotCnt_S")
##
##
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<sqlMap namespace="$namespace">
	
## --------------------------------------------------------------------------------------------------------
	<typeAlias  alias="egovMap" type="egovframework.rte.psl.dataaccess.util.EgovMap"/>
	<typeAlias  alias="${searchVOAlias}" type="${voPackage}.${defaultVoClassName}"/>
	
## --------------------------------------------------------------------------------------------------------
	<resultMap id="$resultMapId" class="$resultMapClass">
#set($i=0)	
#foreach($attribute in $model.attributes)
#set($i=$i+1)	
		<result property="${attribute.ccName}" column="${attribute.name}" columnIndex="$i"/> <!-- ${attribute.columnComment} -->
#end		
	</resultMap>
	
## ##################################################################################################
	<insert id="${insertQueryId}">
		<![CDATA[
			INSERT INTO ${model.entity.name} 
#set($i=0)	
#foreach($attribute in $model.attributes)
#if($i == 0)
				( ${attribute.name} -- ${attribute.columnComment}
#elseif($i < ($model.attributes.size() - 1))
				  , ${attribute.name} -- ${attribute.columnComment}
#else
				  , ${attribute.name} -- ${attribute.columnComment} )
#end
#set($i=$i+1)
#end
##
#set($i=0)	
#foreach($attribute in $model.attributes)
#if($i == 0)
			VALUES ( #${attribute.ccName}# -- ${attribute.columnComment}
#elseif($i < ($model.attributes.size() - 1))
				  , #${attribute.ccName}# -- ${attribute.columnComment}
#else
				  , #${attribute.ccName}# -- ${attribute.columnComment} )
#end
#set($i=$i+1)
#end
		]]>
	</insert>
	
## ##################################################################################################	
	<update id="${updateQueryId}">
		<![CDATA[
			UPDATE ${model.entity.name}
#set($i=0)	
#foreach($attribute in $model.attributes)
#if($i == 0)
			SET ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#else
				, ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#end
#set($i=$i+1)
#end	
##		
#set($i=0)	
#foreach($attribute in $model.primaryKeys)
#if($i == 0)
			WHERE ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#else
				AND ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#end
#set($i=$i+1)
#end
		]]>
	</update>
	
## ##################################################################################################
	<delete id="${deleteQueryId}">
		<![CDATA[
			DELETE FROM ${model.entity.name} 
#set($i=0)	
#foreach($attribute in $model.primaryKeys)
#if($i == 0)
			WHERE ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#else
				AND ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#end
#set($i=$i+1)
#end
		]]>
	</delete>
	
## ##################################################################################################	
	<select id="${selectQueryId}" resultMap="$resultMapId">
		<![CDATA[
			SELECT
#set($i=0)	
#foreach($attribute in $model.attributes)
#if($i == 0)
				${attribute.name} -- ${attribute.columnComment}
#else
				, ${attribute.name} -- ${attribute.columnComment}
#end
#set($i=$i+1)
#end				
			FROM ${model.entity.name}
#set($i=0)	
#foreach($attribute in $model.primaryKeys)
#if($i == 0)
			WHERE ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#else
				AND ${attribute.name}=#${attribute.ccName}# -- ${attribute.columnComment}
#end
#set($i=$i+1)
#end
		]]>
	</select>
	
## ##################################################################################################	
	<select id="${selectListQueryId}" parameterClass="${searchVOAlias}" resultClass="egovMap">
#if($vender == "HSQLDB")
			SELECT
	#set($i=0)	
	#foreach($attribute in $model.attributes)
	#if($i == 0)
					${attribute.name} -- ${attribute.columnComment}
	#else
					, ${attribute.name} -- ${attribute.columnComment}
	#end
	#set($i=$i+1)
	#end	
			FROM ${model.entity.name}
			WHERE 1=1
			<isEqual prepend="AND" property="searchCondition" compareValue="0">
				${model.attributes.get(0).name} = #searchKeyword# -- ${model.attributes.get(0).columnComment}
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="1">
				${model.attributes.get(1).name} LIKE '%' || #searchKeyword# || '%' -- ${model.attributes.get(1).columnComment}
			</isEqual>
				ORDER BY 
	#set($i=0)				
	#foreach($attribute in $model.primaryKeys)
	#if($i == 0)
				${attribute.name} DESC -- ${attribute.columnComment}
	#else
				, ${attribute.name} DESC -- ${attribute.columnComment}
	#end
	#set($i=$i+1)
	#end
			LIMIT #recordCountPerPage# OFFSET #firstIndex#
#elseif($vender == "Oracle")
	       SELECT 
	#set($i=0)	
	#foreach($attribute in $model.attributes)
	#if($i == 0)
					A.${attribute.name} /* ${attribute.columnComment} */
	#else
					, A.${attribute.name} /* ${attribute.columnComment} */
	#end
	#set($i=$i+1)
	#end
	       FROM
            (
				SELECT
	#set($i=0)	
	#foreach($attribute in $model.attributes)
	#if($i == 0)
					${attribute.name} /* ${attribute.columnComment} */
	#else
					, ${attribute.name} /* ${attribute.columnComment} */
	#end
	#set($i=$i+1)
	#end
					, ROWNUM
				FROM ${model.entity.name}
				WHERE 1=1
				<isEqual prepend="AND" property="searchCondition" compareValue="0">
					${model.attributes.get(0).name} = #searchKeyword# /* ${model.attributes.get(0).columnComment} */
				</isEqual>
				<isEqual prepend="AND" property="searchCondition" compareValue="1">
					${model.attributes.get(1).name} LIKE '%' || #searchKeyword# || '%' /* ${model.attributes.get(1).columnComment} */
				</isEqual>
					ORDER BY 
	#set($i=0)				
	#foreach($attribute in $model.primaryKeys)
	#if($i == 0)
				${attribute.name} DESC /* ${attribute.columnComment} */
	#else
				, ${attribute.name} DESC /* ${attribute.columnComment} */
	#end
	#set($i=$i+1)
	#end
			) A
			WHERE ROWNUM BETWEEN #firstIndex# AND #recordCountPerPage#
#elseif($vender == "MySql")
			SELECT
	#set($i=0)	
	#foreach($attribute in $model.attributes)
	#if($i == 0)
					${attribute.name} -- ${attribute.columnComment}
	#else
					, ${attribute.name} -- ${attribute.columnComment}
	#end
	#set($i=$i+1)
	#end
			FROM ${model.entity.name}
			WHERE 1=1
			<isEqual prepend="AND" property="searchCondition" compareValue="0">
				${model.attributes.get(0).name} = #searchKeyword# -- ${model.attributes.get(0).columnComment}
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="1">
				${model.attributes.get(1).name} LIKE CONCAT('%',#searchKeyword#,'%') -- ${model.attributes.get(1).columnComment}
			</isEqual>
				ORDER BY 
	#set($i=0)				
	#foreach($attribute in $model.primaryKeys)
	#if($i == 0)
				${attribute.name} DESC -- ${attribute.columnComment}
	#else
				, ${attribute.name} DESC -- ${attribute.columnComment}
	#end
	#set($i=$i+1)
	#end
			limit #firstIndex#, #recordCountPerPage#	
#else

#end			
	</select>	
## ##################################################################################################	
	<select id="${selectListTotCntQueryId}" parameterClass="${searchVOAlias}" resultClass="int">
#if($vender == "HSQLDB")
			SELECT COUNT(*) totcnt
			FROM ${model.entity.name}
			WHERE 1=1
			<isEqual prepend="AND" property="searchCondition" compareValue="0">
				${model.attributes.get(0).name} = #searchKeyword# -- ${model.attributes.get(0).columnComment}
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="1">
				${model.attributes.get(1).name} LIKE '%' || #searchKeyword# || '%' -- ${model.attributes.get(1).columnComment}
			</isEqual>
#elseif($vender == "Oracle")
			SELECT COUNT(*) totcnt
			FROM ${model.entity.name}
			WHERE 1=1
			<isEqual prepend="AND" property="searchCondition" compareValue="0">
				${model.attributes.get(0).name} = #searchKeyword# /* ${model.attributes.get(0).columnComment} */
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="1">
				${model.attributes.get(1).name} LIKE '%' || #searchKeyword# || '%' /* ${model.attributes.get(1).columnComment} */
			</isEqual>
#elseif($vender == "MySql")
			SELECT COUNT(*) totcnt
			FROM ${model.entity.name}
			WHERE 1=1
			<isEqual prepend="AND" property="searchCondition" compareValue="0">
				${model.attributes.get(0).name} = #searchKeyword# -- ${model.attributes.get(0).columnComment}
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="1">
				${model.attributes.get(1).name} LIKE CONCAT('%',#searchKeyword#,'%') -- ${model.attributes.get(1).columnComment}
			</isEqual>
#else

#end			
	</select>

</sqlMap>
